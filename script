#!./zig-out/bin/kkc


# empty type

Array ^count a

NumProxy ^cock
	NumProxy

fn miau2(_ NumProxy osd)
	osd println()

fn miau (proxy NumProxy ^coo)
	coo println()
	miau2(proxy)

miau(NumProxy as NumProxy 4)
