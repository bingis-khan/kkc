#!./zig-out/bin/kkc
use Term

fn stringu (s): 'miau \(s)'
fn add2(x): x + 2

1 to (10)
	drop(4)
	drop(2)
	map(stringu)
	for-each(Term.println)

1 to (10)
	map(add2)
	take(2)
	for-each(Term.println)


