#!./zig-out/bin/kkc
use List
	add
	remove
	first
	top
	add-all
use Alloc
use Term

al = Alloc.get-c-allocator()
list =& List.mk(al)
list add-all(1 to (100))
list&
	zip(from(0))
	drop(1)
	for-each(Term.println)

list remove(1)

Term.println(list)
Term.println(list first())
Term.println(list top())
