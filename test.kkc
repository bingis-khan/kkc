Int
Bool
	False  # will get assigned 0
	True   # then 1

Unit
	Unit

#[ctype 'const char*']
ConstStr

#[ctype 'void']
Void

#[dylib 'libc.so']
external printf(format ConstStr, x a) -> Void

#[dylib 'libc.so']
external rand() -> Int

#[dylib 'libc.so']
external srand(seed Int) -> Void

#[dylib 'libc.so']
external time(ptr Int) -> Int


Cock
	Cock Int Int Bool Unit
	Miau

t = time(0)
x = srand(t)
x = printf('%d\n', rand())
