# test asdjmlk asjdlkja slk
Unit # kupa
	Unit  # here
			# miau
Int # asdk
Bool
	True #maiu
	False # kasjd

stag (x)
	fn ()
		return x
	return fn

n = stag(1)
n = stag(Unit)


n = Unit
id (x)
	m = n
	return x

n = True
id2 (x)
	m = n
	return x

app(fn, x)
	return fn(x)

r = app(id, 1)
r = app(id2, True)
