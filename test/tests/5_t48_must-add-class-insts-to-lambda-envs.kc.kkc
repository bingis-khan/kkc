#$ Must add class insts to lambda env.
# 1

class Dupa
	dupa (x _) -> I32

inst Dupa I32
	dupa (x) -> I32
		return x

inst Dupa Bool
	dupa (x) -> I32
		if x
			return 1
		else
			return 0

fn f (n)
	return fn: dupa(n)

ff = f(1)
# println(ff)
println(ff())
