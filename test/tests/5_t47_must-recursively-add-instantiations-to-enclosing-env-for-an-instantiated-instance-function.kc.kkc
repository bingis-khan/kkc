#= todo
#$ Must recursively add other instantiations from an instance function.
# 420
#= envsize f 1

class ToInt
	to-int (n _) -> Int

env = 420
fn f ()
	fn g (): env

	inst ToInt Bool
		to-int (n Bool) -> Int
			return g()

	println(to-int(True))
	return

f()
