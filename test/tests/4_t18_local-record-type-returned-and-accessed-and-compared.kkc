#$ Local datatypes with local tvars escaping.
# 123
# ()
# True
# ()
# True

fn f (x a)
	Data b
		extern a
		local b

	return Data { extern: x, local: Unit }

x = f(123)
println(x.extern)
println(x.local)

y = f(True)
println(y.extern)
println(y.local)

z = f(123)
println(@memeq(x, z))
