# 123
# True


fn main ()
	Proxy a
		Proxy

	x = Proxy as Proxy Int  # a function type will not work: (Int -> Int)
	println(123)


	Unchecked
	Checked


	fn check (p Proxy Unchecked) -> Proxy Checked
		return Proxy

	fn makeUnchecked () -> Proxy Unchecked
		return Proxy

	fn consumeChecked (p Proxy Checked) -> Bool
		return True


	uch = makeUnchecked()
	ch = check(uch)
	ch = consumeChecked(ch)
	println(ch)

	return

main()
