#= disabled
#$ [env expansion] Env expansion is not done, because the nested 'gg' function is not called.
# 123

fn uni (l a, r a) -> Unit
	return

f = :123

fn ff (x) -> Unit
	fn gg (y) -> Unit
		fn sumtin ()
			println(x)
			println(y)
			return 123

		uni(f, sumtin)
		return

	return

ff(123)
ff(True)
ff(Unit)

println(f())
