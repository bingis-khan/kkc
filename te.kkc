al = Alloc.idc()
home = Env.get('HOME') or-else('.')
filename = '\(home)/reminders' allocate-str(al)
Term.cprintln('[looking for reminders in \(filename)]', Term.Color16(Term.BrightCyan16), Term.ColorDefault)
