#$ Inner function calling nested outer functions.
# True
# 420

fn id (x)
	return x

fn a ()
	fn aaa (x)
		println(x)
		return x

	fn g ()
		fn f ()
			println(id(True))
			aaa(420)
			return

		fn f2 ()
			f()
			return

		f2()
		return

	g()
	return

a()
